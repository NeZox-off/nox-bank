{
  "name": "nox-bank",
  "private": true,
  "workspaces": [
    "workspaces/*"
  ],
  "scripts": {
    "dev": "lerna run dev --parallel",
    "build": "lerna run build --parallel -no-bail"
  },
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.1.8"
  }
}
